<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("./partials/head") %>
  <title>Forgot Password</title>
  <link rel="stylesheet" href="/css/responsive-forgot-password.css">
</head>
<body style="font-family: 'Segoe UI', sans-serif; background-color: #f8f9fa;">
  <%- include("./partials/nav") %>

  <div class="container d-flex justify-content-center align-items-center py-4" style="min-height: calc(100vh - 100px);">
    <div class="card shadow-lg auth-card rounded-4 p-5" style="width: 100%; max-width: 450px; border: none;">
      <h2 class="text-center fw-bold text-dark mb-4" style="font-size: 2rem;">📧 Forgot Password</h2>

      <% if (typeof errorMessage !== "undefined" && errorMessage) { %>
        <div class="alert alert-danger text-center rounded-3 fw-semibold shadow-sm mb-4" role="alert" style="font-size: 0.95rem;">
          <%= errorMessage %>
        </div>
      <% } %>

      <form action="/user/forgot-password" method="POST">
        <!-- Email -->
        <div class="mb-3 position-relative">
          <label for="email" class="form-label fw-semibold text-secondary">Email address</label>
          <input type="email" class="form-control" id="email" name="email" placeholder="you@example.com" style="padding: 10px; border-radius: 10px;" required>
          <i class="bi bi-envelope-fill" style="
            position: absolute;
            right: 15px;
            top: 70%;
            transform: translateY(-50%);
            color: #6c757d;
            font-size: 1.1rem;
          "></i>
           
        </div>
        <p style="margin-top: 0.3rem; font-size: 0.95rem;padding: 10px; border-radius: 10px;">"OTP might be in spam/promotions folder."</p>

        <!-- Submit Button -->
        <div class="d-grid">
          <button type="submit" class="btn btn-dark btn-lg rounded-3">Send OTP</button>
        </div>
      </form>

      <!-- Back to login -->
      <p class="mt-4 text-center" style="font-size: 0.95rem;">
        Remembered your password? 
        <a href="/user/signin" class="fw-semibold text-decoration-none">Login here</a>
      </p>
    </div>
  </div>

  <%- include("./partials/scripts") %>
  <%- include('./partials/footer') %>
</body>
</html>
